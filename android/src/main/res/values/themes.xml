<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Transparent theme for ZeroSettleHostActivity. The activity hosts a
         Compose ModalBottomSheet; the underlying window must be transparent
         so the sheet floats over the adopter's Flutter content.

         Parent is android:Theme.Translucent.NoTitleBar (framework built-in,
         no Material Components dep required) — the Compose content handles
         its own Material3 theming via ZeroSettleTheme inside onCreate. -->
    <style name="Theme.ZeroSettle.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
