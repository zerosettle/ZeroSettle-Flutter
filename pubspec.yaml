name: zerosettle
description: "ZeroSettle SDK for Flutter — Merchant of Record web checkout."
version: 0.6.0
homepage: https://zerosettle.io
repository: https://github.com/zerosettle/ZeroSettle-Flutter

environment:
  sdk: ^3.10.8
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.zerosettle.flutter
        pluginClass: ZeroSettlePlugin
      ios:
        pluginClass: ZeroSettlePlugin
